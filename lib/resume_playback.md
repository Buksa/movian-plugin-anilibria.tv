# Инструкция по интеграции модуля продолжения просмотра

## 📋 Что нужно сделать

### 1. Обновите файлы

#### `lib/config.js`
Замените ваш текущий config.js на обновленную версию с добавленными настройками:
- `autoResume`
- `findNextEpisode`
- `resumeDelay`
- `showRestartPosition`
- `setFocusOnResume`
- `minPlaycountForResume`
- `minRestartPosition`

#### `lib/resume_playback.js`
Создайте новый файл с кодом модуля продолжения просмотра.

#### `plugin.js`
**В начале файла** добавьте импорт:
```javascript
var resume = require('./lib/resume_playback');
```

**В секции настроек** (после `settings.createString('domain', ...)`) добавьте код из артефакта "Секция настроек для plugin.js".

**В route `moviepage`** замените вызов `FindLastWatched()` на:
```javascript
resume.find(page, page.getItems(), {
  autoResume: config.autoResume,
  findNext: config.findNextEpisode,
  delay: config.resumeDelay,
  showRestartPosition: config.showRestartPosition,
  minPlaycountForResume: config.minPlaycountForResume,
  minRestartPosition: config.minRestartPosition
});
```

**Удалите старую функцию** `FindLastWatched()` - она больше не нужна.

### 2. Структура файлов

```
movian-plugin-anilibria.tv/
├── plugin.js                  ← обновить
├── logo.png
└── lib/
    ├── config.js             ← обновить
    ├── api.js
    ├── ui.js
    ├── log.js
    └── resume_playback.js    ← создать новый
```

## ✨ Что получится

### В настройках плагина появится новый раздел:

```
[Продолжение просмотра:]

ℹ️ Автоматическое определение последнего просмотренного эпизода
   и предложение продолжить со следующего

☐ Автоматически продолжать без вопроса
☑ Продолжать со следующего эпизода
[____1500____] Задержка перед popup (мс)
☑ Показывать позицию продолжения
☐ Устанавливать фокус на эпизод
[____1____] Минимум просмотров для продолжения
[____30____] Минимальная позиция продолжения (сек)
```

### Поведение по умолчанию:

1. Пользователь открывает страницу релиза
2. Страница загружается (1.5 секунды)
3. Модуль ищет последний просмотренный эпизод
4. Если найден следующий эпизод → показывается popup:

```
Вы смотрели: "Gnosia S01E03"

Продолжить со следующего эпизода?
"Gnosia S01E04"

[Нет, остаться здесь]  [Да, продолжить]
```

5. Если нажать "Да" → redirect на следующий эпизод
6. Если нажать "Нет" → остается на странице

## 🎛️ Настройка поведения

### Автоматическое продолжение без вопроса

Включите `Автоматически продолжать без вопроса` в настройках.

**Что изменится:** Popup не показывается, сразу происходит redirect.

### Продолжить с последнего просмотренного (не следующего)

Выключите `Продолжать со следующего эпизода` в настройках.

**Что изменится:** Будет предлагать продолжить тот же эпизод (с позиции `restartpos`).

### Изменить задержку

Измените `Задержка перед popup (мс)` в настройках.

**Рекомендуется:** 1500-2000 мс для стабильной работы.

### Фильтрация по просмотрам

Установите `Минимум просмотров для продолжения` = 2.

**Что изменится:** Будет учитывать только эпизоды просмотренные минимум 2 раза.

### Фильтрация по позиции

Установите `Минимальная позиция продолжения (сек)` = 60.

**Что изменится:** Эпизод считается начатым только если просмотрено минимум 1 минута.

## 🐛 Отладка

Все действия логируются в консоль:

```
[RESUME] Начало поиска последнего просмотренного эпизода
[RESUME] Анализ 8 элементов
[RESUME] Фильтры: minPlaycount=1, minRestartpos=30
[RESUME] Эпизод #0: Gnosia S01E01 (playcount=1, lastplayed=0)
[RESUME] Эпизод #1: Gnosia S01E02 (playcount=0, lastplayed=0)
[RESUME] Эпизод #2: Gnosia S01E03 (playcount=3, lastplayed=1704067200)
[RESUME] Найден следующий эпизод #3: Gnosia S01E04
[RESUME] Найден последний просмотренный: эпизод #2
[RESUME] Следующий эпизод: #3
[RESUME] Показываем popup
[RESUME] Пользователь выбрал: Продолжить
```

Логи доступны в:
- Console DevTools (если есть)
- Лог-файл Movian
- `print()` выводит в оба места

## 🔧 Дополнительные возможности

### Получить информацию без действий

```javascript
var info = resume.info(page.getItems());
if (info) {
  console.log('Последний:', info.title);
  console.log('URL:', info.url);
  console.log('Playcount:', info.playcount);
  console.log('Lastplayed:', info.lastplayed);
  console.log('Restartpos:', info.restartpos);
  console.log('Есть следующий:', info.hasNext);
}
```

### Только установить фокус

```javascript
// На следующий эпизод
resume.focus(page.getItems(), true);

// На последний просмотренный
resume.focus(page.getItems(), false);
```

### Условное использование

```javascript
// Только для сериалов
if (releaseData.type === 'TV') {
  resume.find(page, page.getItems());
}

// Только если больше 3 эпизодов
if (page.getItems().length > 3) {
  resume.find(page, page.getItems());
}
```

## ⚠️ Важные моменты

1. **Задержка обязательна** - метаданные из kvstore загружаются асинхронно, нужно дать им время (1-2 сек).

2. **Фильтры помогают** - `minPlaycount` и `minRestartPosition` предотвращают ложные срабатывания.

3. **Настройки сохраняются** - пользовательские настройки сохраняются между сеансами.

4. **popup.queryOk безопасен** - если пользователь закроет страницу до ответа, ошибки не будет.

5. **Логи помогают отладке** - всегда включены, смотрите их при проблемах.

## 📝 Примеры сценариев

### Сценарий 1: Новый пользователь
- Открывает релиз первый раз
- Эпизодов с `playcount > 0` нет
- Popup не показывается ✅

### Сценарий 2: Досмотрел эпизод
- Просмотрел эпизод 3 полностью
- `playcount = 1`, `lastplayed = timestamp`
- Открывает страницу → предлагает эпизод 4 ✅

### Сценарий 3: Начал смотреть
- Просмотрел 10 секунд эпизода 2
- `playcount = 1`, `restartpos = 10`
- `minRestartPosition = 30` → popup не показывается ✅
- (можно изменить на `0` чтобы показывался)

### Сценарий 4: Последний эпизод
- Досмотрел эпизод 12 (последний)
- Следующего нет → предлагает досмотреть эпизод 12 ✅

## 🎉 Готово!

Теперь ваш плагин автоматически предлагает продолжить просмотр с правильного места!